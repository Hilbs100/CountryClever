<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.countryapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CountryApp">
        <activity android:name=".search_globe"></activity>
        <activity android:name=".world_Map" />
        <activity android:name=".CountryOfDay"/>
        <activity android:name=".QuizGrade"/>
        <activity android:name=".Africa_Map"/>
        <activity android:name=".SouthAmerica_Map"/>
        <activity android:name=".SAresults"/>
        <activity android:name=".NA_Map"/>
        <activity android:name=".Asia_Map"/>
        <activity android:name=".asiaresults"/>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.CountryApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>